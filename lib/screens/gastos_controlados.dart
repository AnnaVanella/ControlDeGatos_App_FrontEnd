import 'package:flutter/material.dart';

class GastosControladosScreen extends StatelessWidget {
  const GastosControladosScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('GastosControladosScreen'),
      ),
    );
  }
}
